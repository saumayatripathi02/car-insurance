version: '3.8'

services:
  backend:
    build: .
    container_name: car-insurance-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    networks:
      - car-insurance-network-be
    command: npm start

networks:
  car-insurance-network-be:
